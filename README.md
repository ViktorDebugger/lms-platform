# Edutrack - LMS Platform

**Edutrack** - це сучасна платформа для навчальних курсів (Learning Management System), яка дозволяє створювати, управляти та проходити онлайн-курси з відео-контентом, платежами та аналітикою.

## 📋 Зміст

- [Про проєкт](#про-проєкт)
- [Основні можливості](#основні-можливості)
- [Технології](#технології)
- [Передумови](#передумови)
- [Встановлення](#встановлення)
- [Налаштування](#налаштування)
- [Запуск](#запуск)
- [Структура проєкту](#структура-проєкту)
- [Розробка](#розробка)
- [Вирішення проблем](#вирішення-проблем)

## 🎯 Про проєкт

Edutrack - це повнофункціональна LMS платформа, розроблена на Next.js 16, яка надає:

- **Для студентів**: Перегляд курсів, відстеження прогресу, покупка курсів через Stripe
- **Для вчителів**: Створення курсів, управління розділами, аналітика продажів
- **Для адміністраторів**: Повний контроль над системою

Платформа підтримує різні ролі користувачів (ADMIN, TEACHER, STUDENT), інтеграцію з платіжними системами, відео-стримінг та завантаження файлів.

## ✨ Основні можливості

### Для студентів
- 🔍 Пошук курсів за назвою та категорією
- 📚 Перегляд курсів з відео-плеєром
- 📊 Відстеження прогресу навчання
- 💳 Покупка курсів через Stripe
- 🎯 Дашборд з прогресом та статистикою

### Для вчителів
- ➕ Створення та редагування курсів
- 📝 Управління розділами та матеріалами
- 🎥 Завантаження відео через Mux
- 📎 Додавання вкладень до курсів
- 💰 Налаштування цін
- 📈 Аналітика продажів та доходів
- 🔄 Переупорядкування розділів (drag & drop)
- 🚀 Публікація/приховування курсів та розділів

### Технічні особливості
- 🔐 Аутентифікація через Clerk
- 💾 База даних PostgreSQL з Drizzle ORM
- 🎨 Сучасний UI на базі shadcn/ui та Tailwind CSS
- 📱 Адаптивний дизайн
- 🌙 Підтримка темної теми
- ⚡ Server Actions для взаємодії з даними
- 🎬 Відео-плеєр Mux для потокового відтворення

## 🛠 Технології

### Frontend
- **Next.js 16** - React фреймворк з App Router
- **React 19** - UI бібліотека
- **TypeScript** - типізація коду
- **Tailwind CSS 4** - стилізація
- **shadcn/ui** - UI компоненти
- **Radix UI** - доступні примітиви
- **TipTap** - rich text редактор
- **React Hook Form** - управління формами
- **Zod** - валідація схем

### Backend
- **Next.js API Routes** - серверні маршрути
- **Drizzle ORM** - робота з базою даних
- **PostgreSQL** - реляційна база даних
- **Server Actions** - серверні дії

### Сервіси та інтеграції
- **Clerk** - аутентифікація та авторизація
- **Stripe** - обробка платежів
- **Mux** - відео стримінг
- **UploadThing** - завантаження файлів
- **Neon Database** - хмарна PostgreSQL (опціонально)

### Інструменти розробки
- **ESLint** - перевірка коду
- **Prettier** - форматування коду
- **Drizzle Kit** - міграції та управління БД

## 📦 Передумови

Перед встановленням переконайтеся, що у вас встановлено:

- **Node.js** версії 18 або вище ([завантажити](https://nodejs.org/))
- **npm**, **yarn** або **pnpm** (менеджер пакетів)
- **PostgreSQL** база даних (локально або хмарна)
- **Git** для клонування репозиторію

### Облікові записи сервісів

Для повнофункціональної роботи потрібні облікові записи та API ключі:

1. **Clerk** - [clerk.com](https://clerk.com) (аутентифікація)
2. **Stripe** - [stripe.com](https://stripe.com) (платежі)
3. **Mux** - [mux.com](https://mux.com) (відео)
4. **UploadThing** - [uploadthing.com](https://uploadthing.com) (файли)

## 🚀 Встановлення

### 1. Клонування репозиторію

```bash
git clone <repository-url>
cd lms-platform
```

### 2. Встановлення залежностей

```bash
npm install
```

або

```bash
yarn install
```

або

```bash
pnpm install
```

### 3. Налаштування змінних оточення

Створіть файл `.env.local` в корені проєкту:

```env
# База даних PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/lms_db"

# Clerk Authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."

# Stripe Payments
STRIPE_API_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Mux Video Streaming
MUX_TOKEN_ID="your_mux_token_id"
MUX_TOKEN_SECRET="your_mux_token_secret"

# UploadThing File Upload
UPLOADTHING_SECRET="sk_live_..."
UPLOADTHING_APP_ID="your_app_id"
```

**Важливо**: Замініть значення на ваші реальні API ключі.

### 4. Налаштування бази даних

#### Варіант A: Використання існуючих міграцій

Якщо у вас вже є міграції в папці `drizzle/`:

```bash
npm run db:push
```

#### Варіант B: Генерація нових міграцій

Якщо потрібно згенерувати міграції зі схеми:

```bash
npm run db:generate
npm run db:migrate
```

## ⚙️ Налаштування

### Clerk (Аутентифікація)

1. Створіть обліковий запис на [clerk.com](https://clerk.com)
2. Створіть новий додаток
3. У налаштуваннях додайте URL для розробки: `http://localhost:3000`
4. Скопіюйте `Publishable Key` та `Secret Key` в `.env.local`
5. Налаштуйте Sign-in/Sign-up методи за потреби

### Stripe (Платежі)

1. Створіть обліковий запис на [stripe.com](https://stripe.com)
2. Перейдіть в **Developers → API keys**
3. Скопіюйте `Secret key` (починається з `sk_test_`) в `STRIPE_API_KEY`
4. Для webhook:
   - Перейдіть в **Developers → Webhooks**
   - Натисніть **Add endpoint**
   - URL: `https://your-domain.com/api/webhook` (для локальної розробки використовуйте Stripe CLI)
   - Виберіть події: `checkout.session.completed`
   - Скопіюйте `Signing secret` в `STRIPE_WEBHOOK_SECRET`

**Локальна розробка з webhook:**
```bash
# Встановіть Stripe CLI
stripe listen --forward-to localhost:3000/api/webhook
# Скопіюйте webhook secret з виводу команди
```

### Mux (Відео)

1. Створіть обліковий запис на [mux.com](https://mux.com)
2. Перейдіть в **Settings → API Access Tokens**
3. Створіть новий токен з правами:
   - Video: Read & Write
4. Скопіюйте `Token ID` та `Token Secret` в `.env.local`

### UploadThing (Файли)

1. Створіть обліковий запис на [uploadthing.com](https://uploadthing.com)
2. Створіть новий додаток
3. Скопіюйте `Secret Key` та `App ID` з налаштувань
4. Налаштуйте розміри та типи файлів за потреби

### PostgreSQL

#### Локальна установка

1. Встановіть PostgreSQL локально
2. Створіть базу даних:
```sql
CREATE DATABASE lms_db;
```
3. Оновіть `DATABASE_URL` в `.env.local`

#### Хмарна база даних (Neon/Supabase)

1. Створіть обліковий запис на [neon.tech](https://neon.tech) або [supabase.com](https://supabase.com)
2. Створіть новий проєкт
3. Скопіюйте connection string в `DATABASE_URL`

## ▶️ Запуск

### Режим розробки

```bash
npm run dev
```

Додаток буде доступний за адресою [http://localhost:3000](http://localhost:3000)

### Продакшен збірка

```bash
npm run build
npm run start
```

### Drizzle Studio (Управління БД)

Для перегляду та управління базою даних через веб-інтерфейс:

```bash
npm run db:studio
```

Відкриється за адресою [http://localhost:4983](http://localhost:4983)

## 📁 Структура проєкту

```
lms-platform/
├── app/                          # Next.js App Router
│   ├── (auth)/                  # Група маршрутів аутентифікації
│   │   └── (routes)/
│   │       ├── sign-in/         # Вхід
│   │       └── sign-up/         # Реєстрація
│   ├── (course)/                # Група маршрутів курсів
│   │   └── courses/
│   │       └── [courseId]/
│   │           ├── chapters/    # Розділи курсу
│   │           └── _components/ # Компоненти курсу
│   ├── (dashboard)/             # Дашборд
│   │   └── (routes)/
│   │       ├── (root)/          # Головна сторінка
│   │       ├── search/          # Пошук курсів
│   │       └── teacher/         # Панель вчителя
│   │           ├── analytics/   # Аналітика
│   │           ├── courses/     # Управління курсами
│   │           └── create/      # Створення курсу
│   └── api/                     # API маршрути
│       ├── courses/             # API курсів
│       ├── uploadthing/         # UploadThing API
│       └── webhook/             # Stripe webhook
├── actions/                     # Server Actions
├── components/                  # React компоненти
│   ├── ui/                      # UI компоненти (shadcn/ui)
│   └── modals/                  # Модальні вікна
├── db/                          # База даних
│   └── schema.ts                # Схема БД (Drizzle)
├── drizzle/                     # Міграції БД
├── hooks/                       # React hooks
├── lib/                         # Утиліти та конфігурації
│   ├── db.ts                    # Підключення до БД
│   ├── stripe.ts                # Stripe конфігурація
│   ├── uploadthing.ts           # UploadThing конфігурація
│   └── utils.ts                 # Загальні утиліти
└── types/                       # TypeScript типи
```

## 💻 Розробка

### Доступні команди

```bash
# Розробка
npm run dev              # Запуск dev сервера

# Збірка
npm run build           # Збірка для продакшену
npm run start           # Запуск продакшен сервера

# База даних
npm run db:generate     # Генерація міграцій
npm run db:migrate      # Застосування міграцій
npm run db:push         # Синхронізація схеми (без міграцій)
npm run db:studio       # Відкриття Drizzle Studio

# Якість коду
npm run lint            # Перевірка ESLint
```

### Робота з базою даних

#### Створення нової таблиці

1. Відредагуйте `db/schema.ts`
2. Згенеруйте міграцію: `npm run db:generate`
3. Застосуйте міграцію: `npm run db:push` (dev) або `npm run db:migrate` (prod)

#### Перегляд даних

Використовуйте Drizzle Studio:
```bash
npm run db:studio
```

### Додавання нових функцій

1. **Створення нових сторінок**: Додайте файли в `app/`
2. **API маршрути**: Створіть `route.ts` в `app/api/`
3. **Server Actions**: Додайте функції в `actions/`
4. **Компоненти**: Створюйте в `components/` або `_components/`

### Тестування Stripe локально

Для тестування платежів локально:

1. Використовуйте тестові ключі Stripe (`sk_test_...`)
2. Для webhook використовуйте Stripe CLI:
```bash
stripe listen --forward-to localhost:3000/api/webhook
```
3. Використовуйте тестові картки Stripe:
   - Успішна оплата: `4242 4242 4242 4242`
   - Відмова: `4000 0000 0000 0002`

## 🔧 Вирішення проблем

### Помилка підключення до бази даних

- Перевірте правильність `DATABASE_URL` в `.env.local`
- Переконайтеся, що PostgreSQL сервер запущений
- Перевірте наявність прав доступу до бази даних
- Для хмарних БД перевірте доступність з вашої IP

### Помилки аутентифікації Clerk

- Перевірте правильність ключів Clerk в `.env.local`
- Переконайтеся, що використовуєте правильний environment (development/production)
- Перевірте налаштування URL в Clerk Dashboard
- Очистіть cookies та спробуйте знову

### Проблеми з завантаженням файлів

- Перевірте налаштування UploadThing
- Переконайтеся, що `UPLOADTHING_SECRET` та `UPLOADTHING_APP_ID` встановлені правильно
- Перевірте розміри та типи файлів (налаштовані в `app/api/uploadthing/core.ts`)

### Проблеми з відео (Mux)

- Перевірте правильність `MUX_TOKEN_ID` та `MUX_TOKEN_SECRET`
- Переконайтеся, що токен має права на читання та запис
- Перевірте формат URL відео (має бути публічний URL)

### Помилки Stripe webhook

- Перевірте `STRIPE_WEBHOOK_SECRET`
- Для локальної розробки використовуйте Stripe CLI
- Перевірте логи Stripe Dashboard для деталей помилок

### Проблеми з міграціями

- Переконайтеся, що база даних доступна
- Перевірте правильність `DATABASE_URL`
- Спробуйте `npm run db:push` замість міграцій для dev середовища

## 📝 Ліцензія

Цей проєкт є приватним та призначений для навчальних цілей.

## 👥 Автори

Розроблено як навчальний проєкт LMS платформи.

---

**Приємної розробки! 🚀**

